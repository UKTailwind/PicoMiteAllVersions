/*
 * ports/web/port_peripherals.h — SPI-LCD + Touch headers for the
 * COMPILE=WEB rp2040 build. GUI.h is intentionally omitted — the
 * rp2040 WEB build doesn't link the GUI controls.
 */

#ifndef PORT_PERIPHERALS_H
#define PORT_PERIPHERALS_H

#include "SSD1963.h"
#include "Touch.h"

#endif /* PORT_PERIPHERALS_H */
