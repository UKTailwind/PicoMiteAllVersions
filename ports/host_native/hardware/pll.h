/* Stub for host build — shared source includes pico-sdk PLL header. No
 * symbols are referenced on host (all uses gated with #ifndef MMBASIC_HOST). */
#ifndef _HARDWARE_PLL_H
#define _HARDWARE_PLL_H
#endif
